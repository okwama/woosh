// Add/Edit Order Page
import 'package:flutter/material.dart';

class TargetsPage extends StatelessWidget {
  const TargetsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Targets'),
      ),
      body: Center(
        child: Text('Targets Page Content'),
      ),
    );
  }
}
