// Add/Edit Order Page
import 'package:flutter/material.dart';

class AddEditOrderPage extends StatelessWidget {
  const AddEditOrderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add/Edit Order'),
      ),
      body: Center(
        child: Text('Add/Edit Order Page Content'),
      ),
    );
  }
}
